<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MainTheme" parent="MainTheme.Base">
        <!-- Instead of a dedicated splash activity, we provide a default background while the app starts up -->
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
    <style name="BlankTheme" parent="MainTheme.Base">
        <item name="android:windowBackground">@drawable/main_screen</item>
    </style>
    <!-- Base theme applied no matter what API -->
    <style name="MainTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
        <item name="windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="windowActionBar">false</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette -->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">#FFFFFF</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">#161826</item>
        <!-- colorAccent is used as the default value for colorControlActivated
         which is used to tint widgets -->
        <item name="colorAccent">#39ADE1</item>
        <item name="android:actionMenuTextColor">#296583</item>
        <item name="android:iconTint">#296583</item>
        <item name="drawerArrowStyle">@style/DrawerIcon</item>
        <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:datePickerDialogTheme">@style/AppCompatDialogStyle</item>
        <item name="android:colorLongPressedHighlight">@android:color/transparent</item>
        <item name="android:colorFocusedHighlight">@android:color/transparent</item>
        <item name="android:colorActivatedHighlight">@android:color/transparent</item>
        <item name="android:activatedBackgroundIndicator">@android:color/transparent</item>
        <item name="android:textAllCaps">false</item>
        <item name="buttonStyle">@style/AndroidButtonStyle</item>
    </style>
    <style name="DrawerIcon" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">#296583</item>
    </style>
    <style name="AppCompatDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#39ADE1</item>
    </style>
    <style name="AndroidButtonStyle" parent="android:Widget.Material.Button">
        <item name="android:minWidth">0dp</item>
    </style>
</resources>